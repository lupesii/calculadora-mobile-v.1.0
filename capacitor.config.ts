import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'calculadora-v.1.0',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
